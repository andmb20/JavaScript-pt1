/*
    IF / ELSE
    IF (se) -> Se ISSO for Verdadeiro, faça ISSO!
    Else (se NÃO) -> Se NÃO, faça ISSO.

    Operadores de comparação
    > maior que 
    < menor que
    == igual que
*/

const notaDeCorte = 7
const notaDoAluno = 6

if (notaDeCorte > notaDoAluno) {
    console.log("PARABENS, Voce passou!!")
}
else {
    console.log("REPROVADO")
}
